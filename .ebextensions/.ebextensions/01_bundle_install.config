commands:
  01_bundle_install:
    command: |
      cd /var/app/current
      gem install bundler -v 2.6.2
      bundle config set --local deployment true
      bundle install --path vendor/bundle
    cwd: /var/app/current